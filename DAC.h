// put prototypes for public functions, explain what it does
// Tyler Anson, Ethan Shen
// 5/5/19

#include <stdint.h>

void DAC_Init(void);							// initialize DAC
void DAC_Out(uint32_t data);			// output data to the DAC to generate sound
